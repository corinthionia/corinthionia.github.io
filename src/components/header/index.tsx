import styled from '@emotion/styled';

const Header = () => {
  return <Wrapper>Github</Wrapper>;
};

const Wrapper = styled.div`
  width: 100%;

  background-image: linear-gradient(60deg, #c6ee61 0%, #f9c5b4 100%);
`;

export default Header;
